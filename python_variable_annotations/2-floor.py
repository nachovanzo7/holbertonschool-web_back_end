#!/usr/bin/env python3

import math

def floor(n: float) -> int:
    """

    Args:
        n (float): numero decimal

    Returns:
        int: devuelve numero redondeado sin decimales
    """
    return math.floor(n)
